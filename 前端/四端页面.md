# 🔌 充电平台四端页面生成提示词

以下为您整理的 **充电平台四端** 完整页面生成提示词，涵盖每个端的核心页面、功能模块和 UI 设计规范。

---

## 一、🖥️ 产品模拟器（Web）

> **定位**：面向硬件开发/测试人员，模拟充电桩设备行为，用于协议联调、功能验证、压力测试。

### 提示词 1：模拟器主控制台

```
请生成一个充电桩产品模拟器的Web主控台页面。
技术栈：Vue3 + Element Plus + ECharts
整体风格：深色科技风（Dark Theme），主色调为深蓝/科技蓝，辅以绿色（正常）、红色（故障）、橙色（告警）状态色。

页面布局：
1. 顶部导航栏：
   - 左侧Logo + "充电桩设备模拟器"标题
   - 右侧显示：当前在线模拟设备数量、服务器连接状态指示灯、系统时间、用户头像

2. 左侧边栏（可折叠）：
   - 设备管理（创建/编辑模拟设备）
   - 协议配置（OCPP 1.6J / OCPP 2.0.1 / 私有协议）
   - 充电流程模拟
   - 故障注入
   - 通信日志
   - 批量压测
   - 场景编排

3. 主工作区 - 设备总览仪表盘：
   - 顶部统计卡片（4列）：模拟设备总数、在线运行中、离线、故障设备数
   - 中部左侧：设备状态分布饼图；中部右侧：实时消息吞吐量折线图（msg/s）
   - 下方：设备列表表格，列包含：设备编号、设备型号、协议版本、当前状态（标签色）、模拟速率、最后心跳时间、操作按钮（启动/停止/配置/删除）
   - 表格支持批量选择、分页、搜索过滤

4. 右下角浮动按钮：快速添加模拟设备（FAB按钮，+号）

要求：响应式布局，表格支持虚拟滚动（可承载1000+设备），关键数据带微动效刷新。
```

### 提示词 2：单设备模拟面板

```
请生成充电桩模拟器的"单设备模拟面板"页面。
风格：深色科技风，信息密度高。

页面布局采用三栏式：
左栏（20%）- 设备基础信息：
   - 设备ID（可编辑输入框）
   - 设备型号（下拉选择：7kW交流/60kW直流/120kW直流/240kW液冷超充）
   - 通信协议（OCPP 1.6J / OCPP 2.0.1）
   - 服务器地址（WebSocket URL 输入框）
   - 连接状态指示灯 + 连接/断开按钮
   - 设备注册（BootNotification）按钮
   - 心跳间隔设置（数字输入，秒）

中栏（50%）- 充电流程模拟：
   - 可视化充电桩状态图（带枪头图标，显示A/B/C三根充电枪状态）
   - 充电状态机流程图（空闲→连接→认证→充电→结束，高亮当前阶段）
   - 充电参数实时面板：电压(V)、电流(A)、功率(kW)、SOC(%)、已充电量(kWh)、充电时长
   - 参数滑块：可手动调节SOC进度、模拟功率曲线
   - 操作按钮组：启动充电、暂停充电、停止充电、发送MeterValues
   - 预设场景下拉：正常充电、BMS异常、过温保护、急停

右栏（30%）- 通信报文监控：
   - 实时报文流（类似终端窗口，带语法高亮）
   - 消息类型过滤标签：全部/发送/接收/错误
   - 单条报文可展开查看JSON详情
   - 报文统计：发送数、接收数、错误数、平均延迟

要求：报文区域支持自动滚动和暂停滚动，参数变更有过渡动画。
```

### 提示词 3：故障注入与场景编排

```
请生成充电桩模拟器的"故障注入与场景编排"页面。
风格：深色科技风，注重操作效率。

上半区 - 故障注入面板：
   - 目标设备选择（多选下拉 + 全选按钮）
   - 故障类型网格卡片（每个卡片带图标和描述）：
     * 通信类：心跳超时、WebSocket断连、消息格式错误
     * 电气类：过压、欠压、过流、漏电、绝缘故障
     * 硬件类：急停按钮触发、枪头过温、模块故障、屏幕异常
     * 业务类：认证失败、计费异常、功率降级
   - 故障参数配置区：延迟触发时间(秒)、持续时长、触发概率(%)
   - 注入方式：单次注入 / 周期注入 / 条件触发
   - 大型"注入故障"按钮（红色警示风格）

下半区 - 场景编排画布：
   - 可视化拖拽式流程编排（类似Node-RED风格）
   - 左侧节点面板：设备节点、动作节点、等待节点、判断节点、循环节点
   - 中间画布：节点连线编排区域，支持缩放和平移
   - 右侧属性面板：选中节点的参数配置
   - 顶部工具栏：保存场景、运行场景、停止场景、导入/导出JSON
   - 底部时间轴：场景运行进度条

要求：故障注入操作需二次确认弹窗，场景画布支持Ctrl+Z撤销。
```

### 提示词 4：批量压测监控

```
请生成充电桩模拟器的"批量压力测试监控"页面。
风格：深色科技风，数据可视化丰富。

顶部配置区（可折叠）：
   - 压测配置：设备数量(1~10000滑块)、并发连接速率(台/秒)、压测持续时间
   - 模拟行为模板：纯心跳 / 心跳+充电 / 全业务模拟
   - 开始压测 / 停止压测 按钮

主监控区（四宫格+底部详情）：
   - 左上：实时连接数曲线图（带峰值标注）
   - 右上：消息吞吐量堆叠面积图（发送/接收/错误分层）
   - 左下：服务器响应延迟分布直方图（P50/P90/P99标注线）
   - 右下：错误率仪表盘（圆环图）+ 错误类型Top5列表
   - 底部：压测时间轴进度条 + 关键里程碑标记（如：500台连接、首次错误等）

底部表格：
   - 异常设备列表：设备ID、异常类型、发生时间、最后报文摘要
   - 支持导出测试报告（PDF/CSV按钮）

要求：所有图表实时更新（WebSocket推送），数字变化带跳动动画，性能指标超过阈值时卡片边框闪烁红色。
```

---

## 二、📱 产品运维（App）

> **定位**：面向现场运维工程师，移动化设备巡检、工单处理、远程操控。

### 提示词 5：运维App首页

```
请生成充电桩运维App的首页界面。
技术栈：Flutter / UniApp，适配iOS和Android
整体风格：简洁专业的工具风格，主色调为品牌蓝(#1677FF)，白色底色，圆角卡片设计。

页面结构（自上而下）：
1. 顶部状态栏：
   - 左侧：运维人员头像 + "你好，张工" 问候语
   - 右侧：消息通知铃铛图标（带红点数字角标）

2. 今日待办数据卡（横向滚动卡片组）：
   - 待处理工单：数字 + "件"
   - 今日巡检：数字 + "站"
   - 紧急告警：数字 + "条"（红色高亮）
   - 设备在线率：百分比 + 环形小图

3. 快捷操作入口（2行×4列图标网格）：
   - 扫码操作（摄像头图标）
   - 我的工单（清单图标）
   - 设备巡检（定位图标）
   - 远程控制（开关图标）
   - 告警中心（警报图标）
   - 备件管理（箱子图标）
   - 知识库（书本图标）
   - 更多功能（九宫格图标）

4. 实时告警滚动列表（最新5条）：
   - 每条包含：告警级别标签(紧急/一般/提醒)、站点名称、告警内容摘要、时间
   - 紧急告警带呼吸灯动画效果
   - 点击可进入工单详情

5. 底部TabBar（5个Tab）：
   - 首页、工单、巡检、消息、我的

要求：首屏加载速度<1s，下拉刷新，告警列表支持左滑操作（转工单/忽略）。
```

### 提示词 6：工单详情页

```
请生成充电桩运维App的"工单详情"页面。
风格：清晰的信息层级，注重现场操作效率。

页面结构：
1. 顶部导航：返回箭头 + 工单编号 + 右上角"更多"菜单

2. 状态进度条（横向步骤条）：
   已创建 → 已接单 → 前往中 → 处理中 → 已完成（当前步骤高亮蓝色）

3. 工单信息卡片：
   - 工单类型标签：故障维修/定期巡检/设备安装/用户投诉
   - 优先级：P0紧急(红)/P1高(橙)/P2中(蓝)/P3低(灰)
   - 站点名称 + 详细地址（可点击跳转导航）
   - 设备编号 + 设备型号
   - 故障描述 + 故障码
   - 创建时间 + 要求完成时间（倒计时显示）
   - 报修人联系方式（一键拨号按钮）

4. 操作区（根据工单状态动态展示）：
   - 前往中状态：大按钮"到达现场"（调用GPS验证定位）
   - 处理中状态：
     * "扫码确认设备"按钮（扫描设备二维码）
     * 故障处理记录（文本输入框 + 拍照上传按钮，最多9张）
     * 更换备件选择（下拉选择 + 数量）
     * "远程重启设备"按钮（带二次确认）
   - 完成状态："提交完工报告"按钮

5. 时间线（操作记录）：
   - 竖向时间轴，记录每个状态变更和操作，含操作人头像、时间、动作描述

6. 底部固定操作栏：
   - 左："联系调度"（电话图标）
   - 中："转派工单"
   - 右："完成工单"（主按钮，蓝色）

要求：所有按钮足够大（最小48dp触控区域），拍照支持水印（时间+地点），弱网环境支持离线暂存。
```

### 提示词 7：设备巡检页面

```
请生成充电桩运维App的"设备巡检"页面。
风格：地图+列表双视图，高效完成巡检任务。

页面包含两个视图切换（顶部Tab切换）：

视图一 - 地图模式：
   - 全屏地图（高德/腾讯地图），显示所有待巡检站点标记点
   - 标记点颜色区分状态：蓝色(待巡检)、绿色(已巡检)、红色(有异常)
   - 点击标记弹出底部站点卡片：站名、设备数量、巡检进度、导航按钮
   - 右下角定位按钮（回到当前位置）

视图二 - 列表模式：
   - 按距离排序的巡检任务列表
   - 每项卡片包含：
     * 站点名称 + 地址
     * 距离（如"1.2km"）
     * 设备数量（如"8台"）
     * 巡检进度条（已检/总数）
     * 最近巡检时间
     * 状态标签

进入巡检执行子页面：
   - 顶部：站点名称 + 进度（如"3/8"）
   - 设备检查清单（逐项打卡）：
     * 外观检查：完好/破损/涂鸦（单选按钮组）
     * 充电枪检查：正常/损坏/缺失
     * 屏幕显示：正常/异常/黑屏
     * 线缆状态：正常/破损/老化
     * 消防设施：在位/缺失/过期
     * 环境照片：拍照上传（强制）
   - NFC打卡按钮（贴近设备NFC标签确认到场）
   - 异常备注输入框

底部固定：提交巡检报告按钮

要求：支持离线巡检（数据本地缓存，联网后自动同步），GPS定位精度提示。
```

### 提示词 8：远程控制台

```
请生成充电桩运维App的"设备远程控制"页面。
风格：操作面板风格，关键操作有安全确认机制。

页面结构：
1. 设备搜索与选择：
   - 搜索框：输入设备编号/站点名称
   - 扫码入口：扫描设备二维码直接进入

2. 设备实时状态面板：
   - 设备3D示意图或扁平插画（显示充电枪状态）
   - 实时参数仪表盘（4个圆形仪表）：
     * 输入电压、输出电流、实时功率、设备温度
   - 运行状态：运行中/待机/故障/离线（大字+状态色）
   - 通信状态：在线/离线 + 最后通信时间

3. 远程控制按钮组（卡片式，每个带图标和说明）：
   - 🔓 远程解锁充电枪（二次确认弹窗）
   - 🔄 远程重启设备（需输入操作密码）
   - ⬆️ 远程升级固件（选择版本号 + 确认）
   - 🔒 锁定/解锁设备
   - ⚡ 调整输出功率（滑块：0%~100%）
   - 📊 下发配置参数（JSON编辑器）

4. 操作日志：
   - 最近20条操作记录列表
   - 每条包含：操作类型、操作人、时间、执行结果（成功/失败标签）

要求：所有控制操作需二次确认+操作密码验证，操作后实时反馈执行结果（loading→成功/失败toast），失败操作自动创建告警工单。
```

---

## 三、🖥️ 后台管理（Web）

> **定位**：面向平台运营管理人员，全局管控充电站运营、用户、财务、数据。

### 提示词 9：运营数据大屏/仪表盘

```
请生成充电平台后台管理系统的"运营数据仪表盘"页面。
技术栈：Vue3 + Element Plus + ECharts/DataV
整体风格：现代商务风，浅色主题，主色#1677FF，数据卡片带轻投影。

页面布局：
1. 顶部信息条：
   - 平台名称"XX充电运营管理平台"
   - 当前时间、天气信息、数据更新时间
   - 全屏切换按钮、主题切换（亮/暗）

2. 核心指标卡片组（一行6个，带环比箭头）：
   - 今日充电量(kWh)  |  今日充电次数  |  今日营收(元)
   - 活跃用户数  |  设备在线率(%)  |  平均利用率(%)
   每个卡片带mini趋势折线图（近7天）

3. 左侧区域（40%宽度）：
   - 上：全国/城市站点分布地图（热力图，可下钻到区县级）
   - 下：近30天营收趋势图（柱状图+折线图组合，电费收入/服务费收入分层）

4. 右侧区域（60%宽度）：
   - 上左：今日充电时段分布（24小时柱状图，标注峰谷平电价时段）
   - 上右：站点利用率排行TOP10（横向柱状图）
   - 中：实时订单流（滚动列表，显示最新充电订单：用户、站点、金额、状态）
   - 下左：充电功率分布饼图（7kW/60kW/120kW/240kW占比）
   - 下右：告警统计（今日新增/待处理/已处理，按类型分布）

5. 底部跑马灯：滚动显示平台公告和运营提示

要求：数据5秒自动刷新，地图支持下钻交互，所有图表带加载骨架屏，适配1920×1080大屏显示。
```

### 提示词 10：站点与设备管理

```
请生成充电平台后台管理的"站点与设备管理"页面。
风格：标准后台管理系统风格，注重数据管理效率。

采用左右分栏布局：

左侧 - 站点树形列表（30%宽度）：
   - 搜索框 + 新增站点按钮
   - 树形结构：城市 → 区域 → 站点
   - 每个站点节点显示：站名、在线设备数/总设备数、状态圆点
   - 右键菜单：编辑、删除、查看、新增子站

右侧 - 站点详情与设备列表（70%宽度）：
   顶部Tab切换：基本信息 | 设备列表 | 电价策略 | 运营数据 | 站点相册

   Tab1-基本信息：
     - 表单式布局：站点名称、地址（地图选点）、经纬度、运营商、联系电话
     - 站点类型：公共站/专用站/内部站
     - 配套设施标签组：卫生间/便利店/停车场/休息室（可添加自定义标签）
     - 营业时间设置（可分工作日/周末）
     - 保存/取消按钮

   Tab2-设备列表：
     - 工具栏：添加设备、批量导入、导出、列设置
     - 数据表格，列：设备编号、型号(交流7kW/直流60kW等)、充电枪数、状态(在线/离线/故障)、固件版本、今日充电量、累计充电量、最近维护日期、操作
     - 操作列：远程控制、编辑、维修记录、报废
     - 设备状态筛选标签：全部/在线/离线/故障

   Tab3-电价策略：
     - 时段电价配置表：时段(00:00-07:00等)、电价类型(谷/平/峰/尖峰)、电费(元/kWh)、服务费(元/kWh)、合计单价
     - 可视化时间轴展示24小时电价分布（不同颜色标注峰谷平）
     - 支持按日/周配置不同策略

要求：表格支持列拖拽排序、行内编辑，地图选点支持POI搜索，大批量数据表格性能优化（虚拟滚动）。
```

### 提示词 11：订单与财务管理

```
请生成充电平台后台管理的"订单与财务管理"页面。
风格：清晰的数据表格+统计分析。

页面包含顶部Tab切换：充电订单 | 退款管理 | 对账管理 | 财务报表

Tab1 - 充电订单：
   - 筛选条件区（可折叠）：
     * 日期范围选择器、站点选择、设备选择、订单状态（进行中/已完成/异常/已退款）
     * 用户手机号搜索、订单号精确搜索、金额范围
   - 数据表格：
     * 列：订单号、用户昵称/手机、站点名称、设备编号、开始时间、结束时间、充电时长(min)、充电量(kWh)、电费、服务费、合计金额、支付方式、订单状态、操作
   - 操作：查看详情、开具发票、发起退款
   - 底部统计栏：筛选结果总订单数、总充电量、总金额
   - 批量导出Excel按钮

Tab2 - 退款管理：
   - 退款申请列表：订单号、用户、退款原因、退款金额、申请时间、审批状态
   - 审批操作：同意/拒绝按钮 + 审批意见输入框

Tab3 - 对账管理：
   - 支付渠道对账（微信/支付宝/银联）
   - 日对账汇总表：日期、渠道、平台订单数、渠道订单数、差异数、差异金额
   - 差异明细展开表格

Tab4 - 财务报表：
   - 月度营收汇总（电费收入、服务费收入、退款金额、净收入）
   - 分站点营收排行表格
   - 导出月度/季度财务报表按钮（PDF格式）

要求：金额数字右对齐并带千分位格式，时间显示精确到秒，大额退款(>100元)需主管审批标记。
```

### 提示词 12：用户与营销管理

```
请生成充电平台后台管理的"用户与营销管理"页面。

分为两大模块（左侧菜单切换）：

模块一 - 用户管理：
   - 用户列表表格：
     * 列：用户ID、昵称、手机号、注册时间、累计充电次数、累计消费金额、余额、会员等级、状态(正常/冻结/注销)、操作
   - 用户详情抽屉（点击用户行从右侧滑出）：
     * 基础信息卡片
     * 充电行为数据（月度充电频次折线图、常用站点Top3）
     * 订单记录列表
     * 优惠券持有情况
     * 操作按钮：调整余额、冻结账号、发送站内信
   - 用户标签管理：可自定义标签（高频用户/低活用户/价格敏感等）

模块二 - 营销中心：
   - 优惠券管理Tab：
     * 优惠券列表：名称、类型(满减/折扣/充电券)、面额、发放量/使用量、有效期、状态
     * 创建优惠券弹窗：完整配置表单
   - 活动管理Tab：
     * 活动列表卡片展示（封面图+标题+时间+状态）
     * 创建新活动按钮
   - 充值活动Tab：
     * 充值档位配置（充100送10、充200送30等）
     * 充值数据统计

要求：用户搜索支持模糊匹配，金额敏感操作需二次确认，营销数据带A/B测试对比视图。
```

### 提示词 13：系统设置与权限

```
请生成充电平台后台管理的"系统设置与权限管理"页面。

左侧菜单树：
   - 角色管理
   - 账号管理
   - 操作日志
   - 系统配置
   - 数据字典
   - API密钥管理

角色管理页：
   - 角色列表表格：角色名、描述、用户数、创建时间、操作
   - 权限配置弹窗：树形勾选菜单权限 + 数据权限（全部/所属区域/所属站点）
   - 预置角色：超级管理员、运营管理员、财务人员、站点管理员、客服人员

账号管理页：
   - 管理员账号列表：姓名、账号、手机号、所属角色、最后登录时间、状态
   - 新增账号表单弹窗
   - 支持强制下线、重置密码操作

操作日志页：
   - 日志表格：操作人、操作模块、操作类型、操作内容摘要、IP地址、时间
   - 高级筛选：按模块/操作人/时间范围/操作类型过滤
   - 日志详情弹窗（完整操作报文）

系统配置页：
   - 表单分区：基础配置、充电配置、支付配置、通知配置、安全配置
   - 每个配置项带说明文字和默认值提示

要求：角色权限变更实时生效，敏感操作(删除用户/修改权限)记录审计日志并标红展示，支持RBAC权限模型可视化配置。
```

---

## 四、📱 用户端（小程序）

> **定位**：面向C端电动车车主，扫码充电、找桩导航、支付结算。

### 提示词 14：小程序首页

```
请生成充电平台用户端微信小程序的首页。
技术栈：原生小程序 / UniApp / Taro
整体风格：清新活力风，主色调绿色(#07C160)+白色，大圆角卡片，符合微信小程序设计规范。

页面结构：
1. 顶部区域：
   - 搜索栏（搜索附近充电站/站点名称），左侧定位图标+当前城市名
   - 右侧扫一扫图标（大按钮，核心入口）

2. 地图区域（占据页面约45%高度）：
   - 腾讯地图，显示用户当前位置（蓝色定位点）
   - 地图上标注附近充电站图标（绿色充电插头标记）
   - 标记点密度适配：远处聚合显示数量气泡，近处展开单独标记
   - 点击标记弹出底部半屏站点卡片
   - 地图右下角：回到定位点按钮、图层切换按钮（标准/卫星）

3. 快捷功能区（横向滚动胶囊按钮）：
   - 🔍 附近站点  |  ⚡ 扫码充电  |  📅 预约充电  |  🏷️ 优惠专区  |  📞 道路救援

4. 推荐站点列表（"附近热门站点"标题）：
   - 每个站点卡片：
     * 站点名称 + 距离（如"800m"）
     * 地址（单行截断）
     * 可用桩数标签（如"空闲 4/8"，绿色文字）
     * 价格标签（如"¥1.2/度"，含服务费）
     * 标签组：快充、24h、停车免费、卫生间
     * 评分星星（如4.8分）
   - 列表支持下拉刷新 + 上拉加载更多

5. 底部TabBar（4个Tab）：
   - 🏠 首页  |  📋 订单  |  💰 钱包  |  👤 我的

要求：地图加载使用骨架屏占位，定位授权引导友好，列表项点击跳转站点详情，整体首屏加载<1.5s。
```

### 提示词 15：扫码充电 + 充电中页面

```
请生成充电平台用户端小程序的"扫码充电流程"和"充电进行中"页面。

页面一 - 扫码结果与启动充电：
   - 顶部：设备编号 + 站点名称
   - 充电桩示意图（显示选择的枪号，如A枪/B枪）
   - 当前电价信息：电费¥X.XX/度 + 服务费¥X.XX/度
   - 充电方式选择：
     * 按金额充电（快捷按钮：¥20/¥30/¥50/自定义输入）
     * 按电量充电（快捷按钮：10度/20度/30度/自定义输入）
     * 充满即停
   - 预估费用提示文字
   - 优惠券选择入口（显示可用券数量）
   - 支付方式选择：微信支付（默认）/ 余额支付
   - 底部大按钮："开始充电"（绿色渐变，占屏幕宽度80%）

页面二 - 充电进行中（核心页面）：
   - 顶部状态栏："充电中"绿色标签 + 站点名称
   - 中央大圆环动画（充电动画）：
     * 圆环内显示：实时SOC百分比（大数字）
     * 圆环进度条随SOC增长，带流光粒子动效
   - 圆环下方4个实时数据：
     * 充电时长(mm:ss)  |  已充电量(kWh)  |  实时功率(kW)  |  已产生费用(元)
   - 电价时段提示："当前为平段电价 ¥1.2/度，14:00将切换至峰段"
   - 充电曲线mini折线图（功率变化趋势）
   - 底部固定操作区：
     * "结束充电"按钮（红色描边按钮，需二次确认弹窗）
     * 紧急停止说明文字
   - 页面支持锁屏后台运行（小程序后台保活提示）

要求：充电数据WebSocket实时推送（1秒/次），圆环动画流畅(60fps)，断网时显示最后缓存数据+断网提示，结束充电后自动跳转支付页面。
```

### 提示词 16：订单列表与详情

```
请生成充电平台用户端小程序的"订单列表"和"订单详情"页面。

订单列表页：
   - 顶部Tab切换：进行中(1) | 已完成 | 退款中
   - 进行中订单卡片（突出显示，绿色边框）：
     * 站点名称 + 设备编号
     * 已充电时长 + 实时费用（动态刷新）
     * "查看详情"按钮 → 跳转充电中页面
   - 已完成订单卡片：
     * 左侧：日期(如"07-13 周一")
     * 中间：站点名称、充电时长+充电量、金额（大字加粗）
     * 右侧箭头
     * 底部标签：支付方式、评价星星
   - 订单列表支持按月份筛选、下拉刷新
   - 空状态插画："暂无充电记录，快去充个电吧~"

订单详情页：
   - 顶部：订单编号（可复制）+ 订单状态
   - 费用明细卡片：
     * 电费：XX度 × ¥X.XX = ¥XX.XX
     * 服务费：XX度 × ¥X.XX = ¥XX.XX
     * 优惠券抵扣：-¥X.XX
     * 实付金额：¥XX.XX（大字）
   - 充电数据卡片：
     * 充电时长、充电电量、开始SOC→结束SOC、最大功率
     * 充电功率曲线图（折线图）
   - 时间信息：开始时间 → 结束时间
   - 站点信息：站点名称 + 地址（可导航）+ 设备编号
   - 底部操作栏：
     * "再次充电"按钮（绿色主按钮）
     * "申请退款"按钮
     * "评价"按钮（如未评价）
     * "开发票"按钮

要求：金额精确到小数点后2位，充电曲线图支持手势缩放查看，退款申请需填写原因弹窗。
```

### 提示词 17：个人中心

```
请生成充电平台用户端小程序的"个人中心"页面。
风格：简洁温暖，注重功能入口展示。

页面结构：
1. 顶部用户信息区（浅色渐变背景）：
   - 用户头像（圆形，支持微信头像）
   - 用户昵称 + 会员等级标签（如"黄金会员 ⭐"）
   - 手机号（脱敏显示：138****8888）
   - 会员积分：XXX分

2. 资产卡片（白色浮层卡片，带阴影）：
   - 三列布局：
     * 账户余额 ¥XX.XX → 充值入口
     * 优惠券 XX张 → 券包入口
     * 积分 XXX → 积分商城入口

3. 车辆管理入口：
   - 已绑定车牌号显示（如"京A·12345"）
   - "+添加车辆" 链接
   - 爱车充电统计：本月充电X次，花费¥XXX

4. 功能列表（cell列表项，带右侧箭头）：
   - 📍 我的收藏站点
   - 📊 充电报告（月度/年度）
   - 🎫 邀请好友（分享赚佣金）
   - 🏷️ 会员权益中心
   - 🔔 消息通知设置
   - 📞 客服与帮助
   - ⭐ 我的评价
   - 📄 发票管理
   - ℹ️ 关于我们
   - ⚙️ 设置

5. 底部：
   - 客服热线电话按钮
   - 版本号信息

要求：头像和昵称通过微信授权获取，车辆信息加密存储，会员等级根据累计充电量自动升级，页面支持分享功能。
```

---

## 📐 通用设计规范提示词（附加）

```
充电平台四端统一设计规范：

【色彩体系】
- 主色：#1677FF（品牌蓝）用于后台管理、运维App
- 辅色：#07C160（充电绿）用于用户端小程序
- 状态色：成功#52C41A、警告#FAAD14、错误#FF4D4F、信息#1677FF
- 深色模式（模拟器专用）：背景#0A1628、卡片#132039、文字#E8F0FE

【字体规范】
- Web端：PingFang SC / Microsoft YaHei
- App端：系统默认字体（iOS: SF Pro / Android: Roboto）
- 小程序：微信默认字体
- 数据数字：DIN Alternate / Roboto Mono（等宽数字）

【间距与圆角】
- Web端基础间距：8px倍数，卡片圆角8px
- App/小程序基础间距：12px，卡片圆角12px，按钮圆角24px
- 最小触控区域：44×44pt（App）、48×48px（Web）

【交互原则】
- 所有危险操作（删除/重启/退款）需二次确认
- 数据加载使用骨架屏，非菊花图
- 列表支持下拉刷新 + 上拉加载
- 表单验证实时反馈（失焦校验）
- 关键状态变更使用微动效过渡

【图标风格】
- Web端：线性图标，2px描边
- App端：双色图标（线+填充）
- 小程序：遵循微信设计规范图标
```

---

以上 **17个提示词** 覆盖了充电平台四端的核心页面。您可以直接将每个提示词输入到 AI 代码生成工具（如 Cursor、v0、Bolt.new 等）中生成对应的页面代码。如需针对某个页面进一步细化或调整，请告诉我！